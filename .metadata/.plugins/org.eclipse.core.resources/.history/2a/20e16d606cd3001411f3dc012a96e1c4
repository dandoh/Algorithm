
public class Exe {
	public static void main(String[] args) {
		In in = new In(args[0]);
		WeightedGraph wg = new WeightedGraph(in);
		
		KruskalMST km = new KruskalMST(wg);
		
		for (Edge e : km.mst()) {
			StdOut.printf("%.0f ", e.weight());
		}
	}
}
