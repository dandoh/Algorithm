
public class Exe {
	public static void main(String[] args) {
		In in = new In(args[0]);
		EdgeWeightedGraph wg = new EdgeWeightedGraph(in);
		KruskalMST km = new KruskalMST(wg);
		
		for (Edge e : km.edges()) {
			StdOut.printf("%.0 ", e.weight());
		}
	}
}
